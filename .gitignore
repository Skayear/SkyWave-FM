# Python
__pycache__/
*.pyc
.venv/
.ruff_cache/
.pytest_cache/

# Secretos
.env
# Generado por scripts/render-icecast-config.sh a partir del .template:
# tiene las passwords de Icecast en texto plano.
/config/icecast.xml

# Biblioteca musical y audio generado (nunca en el repo)
# Con barra inicial: ancla a la raíz del repo, para no matchear
# src/skywave/library/ (el paquete de Python).
/library/
/cache/
assets/ads/*.wav

# Base SQLite generada por `skywave scan` (dato local, no código)
*.db

# Modelos de voz de Piper (~114MB, se bajan con python -m piper.download_voices)
/voices/
